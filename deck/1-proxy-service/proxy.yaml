_format_version: "3.0"
services:
- name: hello-world-${{ env "DECK_USERNAME" }}
  url: http://hello-world-service.app
  routes:
  - name: hello-world-${{ env "DECK_USERNAME" }}
    paths:
    - /${{ env "DECK_USERNAME" }}